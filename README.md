# Java Programming Challenges - Day 1 (Jan 1, 2026)

**Total: 40 Beginner Java Questions**  
*Daily practice for software developer from Narnaund, Haryana. Start with basics, increase difficulty daily. Track progress on GitHub!*

## 📋 Challenge Overview
- **Level**: Beginner (variables, loops, conditionals, strings, arrays)
- **Goal**: Solve all 40 today/tomorrow (20 each day)
- **Time**: 3-7 min per question
- **Tools**: intelliJ IDE + Scanner input

## 📚 Basic Output & Variables (1-10)

1. Print "Happy Coding 2026!" 20 times using while loop.
2. Check if number is positive, negative, or zero.
3. **Sum even numbers** from 1 to user-input N.
4. Find largest of 4 integers using if-else ladder.
5. Print numbers 1 to N in reverse with step 2.
6. Calculate simple interest (P*R*T/100).
7. Swap two numbers without temp variable.
8. Check leap year for user-input year.
9. Print ASCII value of a character.
10. Convert Celsius to Fahrenheit.

## 🔄 Loop Mastery (11-20)

11. Print factorial of N using while loop.
12. Multiplication table 1-12 for given number.
13. Count digits and sum digits of a number.
14. Fibonacci series up to N terms (two ways).
15. Print Armstrong numbers 1-500.
16. Sum of squares 1 to N.
17. Print pyramid pattern (5 rows).
18. Find HCF of two numbers.
19. Reverse digits of a number.
20. Power of a number (base^exponent) using loop.

## 🔤 String Operations (21-30)

21. Reverse string without built-in methods.
22. Check palindrome (ignore case).
23. Count vowels and consonants.
24. Find longest word in sentence.
25. Convert string to uppercase manually.
26. Count word frequency in sentence.
27. Remove duplicate characters.
28. Check anagram of two strings.
29. Extract first non-repeating character.
30. Replace spaces with underscores.

## 📊 Array Fundamentals (31-40)

31. Sum all array elements (user size/input).
32. Find second largest in array.
33. Check if array contains target number.
34. Sort array ascending (bubble sort).
35. Find duplicate elements.
36. Reverse array without extra space.
37. Find missing number (1-N array).
38. Rotate array left by 2 positions.
39. Count even/odd numbers in array.
40. Find max consecutive 1s in binary array.



## 📈 Daily Progression Plan

| Day | Date | Questions | New Topics |
|-----|------|-----------|------------|
| 1 | Jan 1 | 40 | Basics + Arrays |
| 2 | Jan 2 | 45 | Methods + Classes |
| 3 | Jan 3 | 50 | OOP + Inheritance |
| 7 | Jan 7 | 60 | Collections Framework |


# Java Programming Challenges - Day 2 (Jan 2, 2026)

**Total: 45 Intermediate Java Questions**  
*Progressing from Day 1 basics. Now adding Methods, Classes, OOP, Inheritance. Track in GitHub repo!*

## 📋 Challenge Overview
- **Level**: Intermediate (Methods + OOP fundamentals)
- **Goal**: Solve 45 questions over 2 days (22-23 each)
- **Time**: 5-10 min per question
- **Tools**: VS Code + OOP concepts

## 🔧 Methods Mastery (1-15)

1. Write method to find sum of even numbers 1-N (return int).
2. Create method to reverse a string (return String).
3. Write method `isPrime(int n)` returning boolean.
4. Method to calculate factorial using recursion.
5. Method `power(base, exp)` without Math.pow().
6. Method to check palindrome (return boolean).
7. Method to find max of array (return int).
8. Method to count vowels in string (return int).
9. Method `swap(int a, int b)` using pass-by-value demo.
10. Method to generate Fibonacci nth term.
11. Method overloading: `add(int)` and `add(int,int)`.
12. Method to sort array using bubble sort (void).
13. Method `isArmstrong(int n)` returning boolean.
14. Method to find HCF of two numbers.
15. Varargs method to find average of numbers.

## 🏗️ Classes & Objects (16-30)

16. Create `Student` class with name, roll, marks (getter/setter).
17. `Rectangle` class with length, width, area(), perimeter().
18. `BankAccount` class with deposit(), withdraw(), getBalance().
19. `Book` class with title, author, pages, displayInfo().
20. `Employee` class with id, name, salary, annualSalary().
21. Constructor overloading in `Circle` class (radius).
22. `Car` class with model, speed, accelerate(), brake().
23. `Fraction` class with num, den, simplify(), add().
24. `Time` class with hours, mins, display(), addMinutes().
25. Static method `getCircleCount()` in Circle class.
26. `Person` class with name, age, this keyword usage.
27. `Temperature` class with C/F conversion methods.
28. `Matrix` class with add() for two matrices.
29. Final variable PI in Circle class.
30. `StringBuilder` class demo with methods.

## 👥 OOP & Inheritance (31-45)

31. `Vehicle` parent + `Car` child with override toString().
32. `Shape` abstract class + `Circle`, `Rectangle` subclasses.
33. `Animal` hierarchy: `Dog`, `Cat` with `makeSound()`.
34. `Bank` with `SavingsAccount`, `CurrentAccount` subclasses.
35. `Employee` base + `Manager`, `Developer` with salary calc.
36. Multiple inheritance via interfaces: `Drawable`, `Resizable`.
37. `super` keyword to call parent constructor.
38. Method overriding: `Vehicle` move() in `Bike`, `Car`.
39. Protected members access in inheritance.
40. `Student` implements `Comparable<Student>` by marks.
41. Interface `Flyable` implemented by `Bird`, `Airplane`.
42. `GeometricObject` with `equals()` override.
43. `Exception` handling in custom `Calculator` class.
44. `ArrayList` operations in `StudentManager` class.
45. `HashMap` for `PhoneBook` with addContact(), findContact().



# Java Programming Challenges - Day 3 (Jan 3, 2026)

**Total: 50 Advanced Intermediate Java Questions**  
*Day 3 progression: Collections Framework, Exceptions, Multithreading basics. Building full applications now!*

## 📋 Challenge Overview
- **Level**: Advanced Intermediate (Collections + Exception Handling)
- **Goal**: Solve 50 questions over 2-3 days (15-20 daily)
- **Time**: 7-15 min per question
- **Tools**: VS Code + Java Collections

## 🗂️ Collections Framework (1-20)

1. Create `ArrayList<Integer>` and find max element.
2. `LinkedList` operations: addFirst(), addLast(), remove().
3. `HashSet` to remove duplicates from array.
4. `TreeSet` for sorted unique student names.
5. `HashMap<String, Integer>` word frequency counter.
6. `TreeMap` for sorted employee salaries.
7. Iterate `ArrayList` using enhanced for loop.
8. Convert `ArrayList` to `Array`.
9. `PriorityQueue` for task scheduling (min heap).
10. `Stack` operations: push, pop, peek for undo feature.
11. `Queue` using LinkedList: enqueue, dequeue.
12. Custom `Comparator` for sorting students by marks.
13. `HashMap` phone book with add/find contacts.
14. Merge two sorted `ArrayList`s.
15. Find intersection of two `HashSet`s.
16. `ArrayList` of custom `Student` objects with equals().
17. `LinkedHashMap` to maintain insertion order.
18. Count occurrences using `HashMap<Character, Integer>`.
19. `TreeSet` of custom `Product` with comparable.
20. Remove nulls from `ArrayList` using filter.

## ⚠️ Exception Handling (21-30)

21. Custom exception `InvalidAgeException` for age < 0.
22. Divide by zero handling with try-catch-finally.
23. `NumberFormatException` handling for string to int.
24. `ArrayIndexOutOfBoundsException` in loop.
25. Multiple catch blocks with `ArithmeticException`.
26. `throws` declaration in custom method.
27. `FileNotFoundException` handling (simulate).
28. `NullPointerException` prevention in methods.
29. Nested try-catch for database operations.
30. `finally` block always executes demo.

## 🔄 Multithreading Basics (31-40)

31. Extend `Thread` class for counter task.
32. `Runnable` interface for printer task.
33. `Thread` with `join()` synchronization.
34. `synchronized` method for shared counter.
35. Producer-Consumer with `wait()` and `notify()`.
36. `Thread.sleep()` for delay simulation.
37. Multiple threads printing numbers 1-100.
38. `ExecutorService` with thread pool.
39. `Callable` and `Future` for threaded sum.
40. `ThreadLocal` for thread-specific variables.

## 🏗️ Advanced OOP (41-50)

41. Abstract `DatabaseConnection` with concrete subclasses.
42. Interface `PaymentGateway` + `PayPal`, `Razorpay`.
43. Enum `DayOfWeek` with switch case usage.
44. Generic `Box<T>` class for type safety.
45. `List<Student>` sorting with `Collections.sort()`.
46. Singleton `Logger` class pattern.
47. Factory pattern for `Shape` creation.
48. `StringBuilder` vs `StringBuffer` performance.
49. `Object` class methods override: hashCode(), equals().
50. Inner class for event handler in GUI simulation.

